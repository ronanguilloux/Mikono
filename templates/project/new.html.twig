{% extends 'base.html.twig' %}

{% block title %}Add project — UCESCO Volunteer Manager{% endblock %}

{% block body %}
    <h1 class="mb-4 text-xl font-semibold">Add project</h1>
    {# Form attributes (width, Stimulus wiring) come from ProjectFormType. #}
    <twig:FormShell :form="form" cancelUrl="{{ path('project_index') }}" :formAttr="{}" />
{% endblock %}
