{% extends 'base.html.twig' %}

{% block title %}Edit {{ program.name }} — UCESCO Volunteer Manager{% endblock %}

{% block body %}
    <h1 class="mb-4 text-xl font-semibold">Edit {{ program.name }}</h1>
    <twig:FormShell :form="form" cancelUrl="{{ path('program_index') }}" :formAttr="{}" />
{% endblock %}
